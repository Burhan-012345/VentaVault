# VantaVault 🔐

A futuristic, high-security Progressive Web App (PWA) for hiding photos and videos with local-first encryption.

## ✨ Features

- **PIN & Fingerprint Only** - No usernames, emails, or passwords
- **AES-256 Encryption** - All files encrypted before storage
- **Fake Vault System** - Decoy mode with dummy data
- **Progressive Web App** - Installable, works offline
- **Local-First** - No cloud services, complete privacy
- **Secure Sharing** - Temporary, expiring share links
- **Auto-Lock** - Inactivity timeout and stealth mode

## 🚀 Quick Start

### 1. Installation

```bash
# Clone or extract the project
cd vantavault

# Create virtual environment (recommended)
python -m venv venv

# Activate virtual environment
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt